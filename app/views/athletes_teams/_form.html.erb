<%= form_for(@athletes_team) do |f| %>
  <% if @athletes_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@athletes_team.errors.count, "error") %> prohibited this athletes_team from being saved:</h2>

      <ul>
      <% @athletes_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :athlete_id %><br>
    <%= f.number_field :athlete_id %>
  </div>
  <div class="field">
    <%= f.label :captain %><br>
    <%= f.check_box :captain %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
